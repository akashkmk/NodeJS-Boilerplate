import mongoose from 'mongoose';
import ServiceASchema from './schema';

const ServiceAModel = mongoose.model(ServiceASchema);

//* * Model related Other works will be here */
export default ServiceAModel;
